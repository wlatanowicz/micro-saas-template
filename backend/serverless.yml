service: micro-saas-app

frameworkVersion: "3"

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    fileName: requirements-lambda.txt
    dockerizePip: non-linux
    slim: true
    strip: false
    noDeploy:
      - boto3
      - botocore

  # Custom hostname + ACM are optional. Set BOTH env vars for a custom domain; otherwise CloudFront
  # uses the default *.cloudfront.net certificate (no Aliases). The ACM cert must be in us-east-1
  # and include this hostname in SAN, or creation fails with an invalid CNAME error.
  frontendDomainName: ${env:FRONTEND_DOMAIN_NAME, ''}
  frontendAcmCertArn: ${env:FRONTEND_ACM_CERT_ARN, ''}

provider:
  name: aws
  runtime: python3.11
  region: ${opt:region, 'eu-central-1'}
  stage: ${opt:stage, 'prod'}
  memorySize: 512
  timeout: 30
  httpApi:
    cors:
      allowedOrigins:
        - "*"
      allowedHeaders:
        - Content-Type
        - Authorization
      allowedMethods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
  environment:
    DATABASE_URL: ${env:DATABASE_URL, ''}
  # VPC for all Lambdas. Prefer editing lambda-vpc.config.json (see lambda-vpc.config.example.json), or replace
  # this line with a literal block: vpc: { subnetIds: [...], securityGroupIds: [...] }.
  # When lambda-vpc.config.json is absent, lambda-vpc.no-vpc.json (false) disables VPC.
  vpc: ${file(./lambda-vpc.config.json), file(./lambda-vpc.no-vpc.json)}

functions:
  api:
    handler: src.handler.handler
    package:
      patterns:
        - "!alembic/**"
        - "!alembic.ini"
        - "!src/migrate_handler.py"
    events:
      - httpApi:
          path: /
          method: ANY
      - httpApi:
          path: /{proxy+}
          method: ANY

  migrate:
    handler: src/migrate_handler.handler
    timeout: 120
    memorySize: 512
    description: Alembic upgrade head (invoke after deploy)

package:
  individually: true
  patterns:
    - "!venv/**"
    - "!.venv/**"
    - "!tests/**"
    - "!**/__pycache__/**"
    - "!*.pyc"
    - "!pyproject.toml"
    - "!uv.lock"
    - "!requirements-lambda.txt"
    - "!.ruff_cache/**"

resources:
  Conditions:
    HasFrontendCustomDomain:
      Fn::And:
        - Fn::Not:
            - Fn::Equals:
                - "${self:custom.frontendDomainName}"
                - ""
        - Fn::Not:
            - Fn::Equals:
                - "${self:custom.frontendAcmCertArn}"
                - ""

  Resources:
    FrontendBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: !Sub "${self:service}-${self:provider.stage}-frontend-${AWS::AccountId}"
        WebsiteConfiguration:
          IndexDocument: index.html
          ErrorDocument: index.html
        PublicAccessBlockConfiguration:
          BlockPublicAcls: false
          IgnorePublicAcls: false
          BlockPublicPolicy: false
          RestrictPublicBuckets: false

    FrontendBucketPolicy:
      Type: AWS::S3::BucketPolicy
      Properties:
        Bucket: !Ref FrontendBucket
        PolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Principal: "*"
              Action: s3:GetObject
              Resource: !Sub "${FrontendBucket.Arn}/*"

    FrontendCloudFrontDistribution:
      Type: AWS::CloudFront::Distribution
      Properties:
        DistributionConfig:
          Enabled: true
          Comment: !Sub "${self:service}-${self:provider.stage} frontend"
          Aliases:
            Fn::If:
              - HasFrontendCustomDomain
              - - "${self:custom.frontendDomainName}"
              - Ref: AWS::NoValue
          Origins:
            - Id: FrontendS3WebsiteOrigin
              DomainName: !Sub "${FrontendBucket}.s3-website.${AWS::Region}.amazonaws.com"
              CustomOriginConfig:
                HTTPPort: 80
                HTTPSPort: 443
                OriginProtocolPolicy: http-only
          DefaultCacheBehavior:
            TargetOriginId: FrontendS3WebsiteOrigin
            ViewerProtocolPolicy: redirect-to-https
            AllowedMethods:
              - GET
              - HEAD
              - OPTIONS
            CachedMethods:
              - GET
              - HEAD
            Compress: true
            ForwardedValues:
              QueryString: false
              Cookies:
                Forward: none
          DefaultRootObject: index.html
          CustomErrorResponses:
            - ErrorCode: 403
              ResponseCode: 200
              ResponsePagePath: /index.html
            - ErrorCode: 404
              ResponseCode: 200
              ResponsePagePath: /index.html
          PriceClass: PriceClass_100
          ViewerCertificate:
            Fn::If:
              - HasFrontendCustomDomain
              - AcmCertificateArn: "${self:custom.frontendAcmCertArn}"
                SslSupportMethod: sni-only
                MinimumProtocolVersion: TLSv1.2_2021
              - CloudFrontDefaultCertificate: true

  Outputs:
    FrontendBucketName:
      Value: !Ref FrontendBucket
    FrontendWebsiteURL:
      Value: !GetAtt FrontendBucket.WebsiteURL
    FrontendCloudFrontDomainName:
      Value: !GetAtt FrontendCloudFrontDistribution.DomainName
    FrontendCloudFrontDistributionId:
      Value: !Ref FrontendCloudFrontDistribution
    FrontendCustomDomainName:
      Value:
        Fn::If:
          - HasFrontendCustomDomain
          - "${self:custom.frontendDomainName}"
          - ""

    MigrateLambdaName:
      Description: Physical name of the migration Lambda (invoke after deploy)
      Value:
        Ref: MigrateLambdaFunction
